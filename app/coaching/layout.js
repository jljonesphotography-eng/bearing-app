export const metadata = {
  title: 'Coaching Programs | Bearing',
  description: 'Capability-calibrated coaching built on your assessment — not generic coaching.'
};

export default function CoachingLayout({ children }) {
  return children;
}
